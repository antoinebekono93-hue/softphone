import { ChannelsApprovalClient } from "./ChannelsApprovalClient";

export const metadata = {
  title: "Rich Messaging Approvals | God Mode",
};

export default function GodModeChannelsPage() {
  return <ChannelsApprovalClient />;
}
